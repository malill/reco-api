# this folder contains connection infos and environmental settings (dev|staging|production)
# structure:

ENVIRONMENT = "DEMO"

# version
VERSION = *.*
ENDPOINT_STR = "/api/v*"

# database settings
DB_HOST = ***********
DB_PORT = ****
DB_NAME = ***********
DB_USER = ***********
DB_PASSWORD = ***********